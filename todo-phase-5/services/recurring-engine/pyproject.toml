[project]
name = "donekaro-recurring-engine"
version = "1.0.0"
description = "DoneKaro Recurring Engine - Task scheduling and recurrence"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
